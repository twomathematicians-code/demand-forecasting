#!/usr/bin/env python3
"""ml-demand-forecasting ML pipeline."""
import sys
from pathlib import Path
sys.path.insert(0, str(Path(__file__).parent.parent))

if __name__ == "__main__":
    print("Demand Forecasting API pipeline — implement domain logic here.")
