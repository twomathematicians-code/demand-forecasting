# Lightweight requirements for fast Render free tier deploys
# Excludes heavy packages (torch, evidently, aiokafka) not needed on free tier
# Full deps: requirements.txt

fastapi>=0.111
uvicorn[standard]>=0.30
pydantic>=2.7
pydantic-settings>=2.3
lightgbm>=4.3
statsmodels>=0.14
prophet>=1.1
scikit-learn>=1.5
pandas>=2.2
numpy>=1.26
httpx>=0.27
pyyaml>=6.0
joblib>=1.4
asyncpg>=0.29
sqlalchemy>=2.0
alembic>=1.13
datasets>=2.18
# torch — skip on free tier (800MB, CNN-LSTM disabled)
# aiokafka — skip on free tier
# evidently — skip on free tier
# apscheduler — skip on free tier
# redis — skip on free tier
